name: Auto Release APK

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - '**.md'
      - '.gitignore'

jobs:
  build_and_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Calculate Next Version Tag
        id: version
        run: |
          LATEST_TAG=$(git describe --tags --match "*1.0.*" --abbrev=0 2>/dev/null || echo "1.0.-1")
          IFS='.' read -ra ADDR <<< "$LATEST_TAG"
          X=${ADDR[2]}
          NEW_X=$((X+1))
          NEW_TAG="1.0.$NEW_X"
          echo "tag=$NEW_TAG" >> $GITHUB_OUTPUT
          
          # Rename APK to a fixed name so the 'latest' download link always works
          mv app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/app-release.apk

      - name: Create Release and Upload APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: release ${{ steps.version.outputs.tag }}
          body: ""
          files: app/build/outputs/apk/debug/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
